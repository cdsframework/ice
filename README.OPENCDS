Build Order

Build opencds-knowledge-repository-data
mvn clean install

To build OpenCDS with other ICE, do the following:
mvn -P drools-7 -Dmaven.test.skip=true clean install

The war file will be in the target folder.
